#!/usr/bin/env bash
# Release {{.Name}} as a Homebrew {{.Kind}} via jwa-tobrew.
#
# Usage:
#   ./scripts/release.sh <version> <path/to/artifact>
#
# Example:
#   ./scripts/release.sh 0.5.0 build/{{.Name}}.dmg
#
# Prereqs: jwa-tobrew, gh, op (1Password CLI), 1Password signed in.

set -euo pipefail

if [[ $# -lt 2 ]]; then
  echo "usage: $0 <version> <artifact>" >&2
  exit 2
fi

cd "$(dirname "$0")/.."

jwa-tobrew release \
  --kind {{.Kind}} \
  --name {{.Name}} \
  --version "$1" \
  --asset "$2"
