# Resolved by `jwa-harden run -- <command>` (or `op run --env-file=.env.template --`).
# Values are 1Password references — the resolved secrets exist only in the
# spawned process tree, never on disk. See ~/dotfiles/docs/security-ground-rules.md.

# Tap-writer PAT — scoped to the homebrew-tap repo only.
GITHUB_TOKEN=op://Personal/GitHub Homebrew-tap writer/credential
HOMEBREW_TAP_GITHUB_TOKEN=op://Personal/GitHub Homebrew-tap writer/credential
