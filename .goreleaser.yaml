version: 2

project_name: jwa-tobrew

before:
  hooks:
    - go mod tidy

builds:
  - id: jwa-tobrew
    main: ./cmd/jwa-tobrew
    binary: jwa-tobrew
    env:
      - CGO_ENABLED=0
    goos: [darwin, linux]
    goarch: [amd64, arm64]
    ldflags:
      - -s -w
      - -X main.version={{ .Version }}
      - -X main.commit={{ .ShortCommit }}
      - -X main.date={{ .Date }}

archives:
  - id: default
    ids: [jwa-tobrew]
    formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc
  use: github
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
      - "Merge pull request"
  groups:
    - title: Features
      regexp: "^.*feat(\\(.+\\))??!?:.+$"
      order: 0
    - title: Bug fixes
      regexp: "^.*fix(\\(.+\\))??!?:.+$"
      order: 1
    - title: Other
      order: 999

release:
  github:
    owner: jwa91
    name: jwa-tobrew
  draft: false
  prerelease: auto

# Modern Homebrew distribution for pre-compiled binaries (ADR 0008 in
# jwa91/homebrew-tap). Publishes Casks/jwa-tobrew.rb back to the tap.
homebrew_casks:
  - name: jwa-tobrew
    ids: [default]
    repository:
      owner: jwa91
      name: homebrew-tap
      branch: main
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    directory: Casks
    homepage: "https://github.com/jwa91/jwa-tobrew"
    description: "Release a project to the personal jwa91/tap Homebrew tap"
    license: MIT
    binaries: [jwa-tobrew]
    dependencies:
      - formula: gh
    commit_author:
      name: goreleaserbot
      email: bot@goreleaser.com
    commit_msg_template: "feat(jwa-tobrew): cask update to {{ .Tag }}"
